global:
  scrape_interval: 1s  # How often to scrape targets by default
  evaluation_interval: 1s  # How often to evaluate rules

scrape_configs:
  - job_name: 'myjob'
    static_configs:
      - targets: ['10.0.0.20:9100', '10.0.0.21:9100']